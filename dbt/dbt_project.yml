name: northstar_finance
version: '1.0.0'
config-version: 2
profile: northstar_finance
model-paths: ["models"]
test-paths: ["tests"]
models:
  northstar_finance:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
vars:
  as_of_date: '2025-05-15'
